{
  "name": "cole",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "browser-sync start --server --files '**/*.css, **/*.html, !node_modules/**/*' --directory --port 1234 --browser"
  },
  "devDependencies": {
    "browser-sync": "^2.26.7"
  }
}
